#pragma once

#define 整数 int
#define 主函数 main
#define 返回 return
#define 计次循环(count) for(int 次数 = 0; 次数 < count; 次数 ++)
#define 计次循环首(count) 计次循环(count) {
#define 计次循环尾 }
#define 判断循环(condition) while(condition)
#define 判断循环首(condition) 判断循环(condition) {
#define 判断循环尾 }
#define 循环判断首 do{
#define 循环判断尾(condition) } while(condition);
#define 如果 if
#define 否则 else
#define 又如 else if